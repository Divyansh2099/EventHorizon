# examples

**Responsibility:**
Contains reference implementations demonstrating how to embed and configure the EventHorizon server.

**Contents:**
- `basic/`: A simple "Hello World" HTTP server.
- `routing/`: Demonstrates the high-performance lock-free router.
- `middleware/`: Demonstrates how to inject zero-allocation middleware.
